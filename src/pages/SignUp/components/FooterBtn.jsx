import Button from '@components/Button/Button';

const FooterBtn = () => {
  return (
    <div className="sign-up-footer-wrap">
      <Button type="submit" content="회원가입" color="primary" size="large" />
    </div>
  );
};

export default FooterBtn;
